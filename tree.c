#include <assert.h>
#include "cc.h"

// simplify a root expression
struct expr *reduce(struct expr *expr)
{
    // TODO:
    return expr;
}

// get the address
struct expr *addrof(struct expr *expr)
{
    // TODO:
    return NULL;
}
