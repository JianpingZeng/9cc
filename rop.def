
_rop(IR_NONE,           "null",         NULL)

// control
_rop(IR_LABEL,          "label",        NULL)
_rop(IR_GOTO,           "goto",         NULL)
_rop(IR_IF_I,           "if",           use_if)
_rop(IR_IF_F,           "if",           use_if)
_rop(IR_IF_FALSE_I,     "ifFalse",      use_if)
_rop(IR_IF_FALSE_F,     "ifFalse",      use_if)
_rop(IR_RETURNI,        "return",       use_uop)
_rop(IR_RETURNF,        "return",       use_uop)

// binary

// arith
_rop(IR_ADDI,           "+",            use_bop)
_rop(IR_ADDF,           "+",            use_bop)

_rop(IR_SUBI,           "-",            use_bop)
_rop(IR_SUBF,           "-",            use_bop)

_rop(IR_DIVI,           "/",            use_bop)
_rop(IR_IDIVI,          "/",            use_bop)
_rop(IR_DIVF,           "/",            use_bop)

_rop(IR_MULI,           "*",            use_bop)
_rop(IR_IMULI,          "*",            use_bop)
_rop(IR_MULF,           "*",            use_bop)

// integer
_rop(IR_MOD,            "%",            use_bop)
_rop(IR_OR,             "|",            use_bop)
_rop(IR_AND,            "&",            use_bop)
_rop(IR_XOR,            "^",            use_bop)
_rop(IR_LSHIFT,         "<<",           use_bop)
_rop(IR_RSHIFT,         ">>",           use_bop)

_rop(IR_SUBSCRIPT,      "[]",           NULL)
_rop(IR_ASSIGNI,        "=",            use_uop)
_rop(IR_ASSIGNF,        "=",            use_uop)

// unary
_rop(IR_ADDRESS,        "&",            NULL)
_rop(IR_INDIRECTION,    "*",            NULL)
_rop(IR_NOT,            "~",            use_uop)
_rop(IR_MINUSI,         "-",            use_uop)
_rop(IR_MINUSF,         "-",            use_uop)

// function
_rop(IR_PARAM,          "param",        use_uop)
_rop(IR_CALL,           "call",         use_uop)

// conv
_rop(IR_CONV_UI_UI,     "uint => uint",         use_uop)
_rop(IR_CONV_SI_SI,     "int => int",           use_uop)
_rop(IR_CONV_UI_SI,     "uint => int",          use_uop)
_rop(IR_CONV_SI_UI,     "int => uint",          use_uop)

_rop(IR_CONV_SI_F,      "int => float",         use_uop)
_rop(IR_CONV_UI_F,      "uint => float",        use_uop)

_rop(IR_CONV_FF,        "float => float",       use_uop)

_rop(IR_CONV_F_UI,      "float => uint",        use_uop)
_rop(IR_CONV_F_SI,      "float => int",         use_uop)

#undef _rop